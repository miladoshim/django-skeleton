info:
  name: otp verify
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseurlv1}}/auth/login/"
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: "09376686365"
      - name: password
        type: text
        value: "12344321"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
