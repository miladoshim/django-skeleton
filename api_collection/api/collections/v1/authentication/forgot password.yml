info:
  name: forgot password
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseurlv1}}/auth/password/forgot/"
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: "09376686365"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
