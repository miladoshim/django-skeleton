info:
  name: forgot password reset
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseurlv1}}/auth/password/reset/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
