info:
  name: password change
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseurlv1}}/accounts/password_change/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
