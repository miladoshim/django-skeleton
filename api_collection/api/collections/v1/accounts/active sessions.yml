info:
  name: active sessions
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseurlv1}}accounts/sessions/"
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: adminuser
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
