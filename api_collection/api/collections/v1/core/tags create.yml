info:
  name: tags create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseurlv1}}tags/"
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: new
      - name: slug
        type: text
        value: newcourse
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
