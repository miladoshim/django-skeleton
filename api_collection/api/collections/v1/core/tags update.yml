info:
  name: tags update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseurlv1}}tags/2/"
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: test
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
